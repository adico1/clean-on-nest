import { AccountsErrorInterceptor } from './accounts-error.interceptor';

describe('AccountsErrorInterceptor', () => {
  it('should be defined', () => {
    expect(new AccountsErrorInterceptor()).toBeDefined();
  });
});
